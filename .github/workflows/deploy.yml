name: 🚀 Deploy

on:
  push:
    branches:
      - main
      - dev
  pull_request: {}

permissions:
  actions: write
  contents: read

jobs:
  lint:
    name: 🧹 Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1
      - name: Install dependencies
        run: npm install

  typecheck:
    name: 🔎 Type check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1
      - name: Install dependencies
        run: npm install

  build:
    name: 🐳 Build
    uses: ./.github/workflows/build.yml
    secrets: inherit

  deploy:
    name: 🚀 Deploy
    runs-on: [self-hosted]
    needs: [lint, typecheck, build]
    if: ${{ github.ref == 'refs/heads/main' && github.event_name == 'push' }}
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@v4.1.1

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 📝 Create .env file
        run: |
          echo "PGUSER=${{ secrets.PGUSER }}" > .env
          echo "PGHOST=${{ secrets.PGHOST }}" >> .env
          echo "PGDATABASE=${{ secrets.PGDATABASE }}" >> .env
          echo "PGPASSWORD=${{ secrets.PGPASSWORD }}" >> .env
          echo "PGPORT=${{ secrets.PGPORT }}" >> .env
          echo "PORT=8888" >> .env

      - name: 📂 Afficher le répertoire courant
        run: pwd

      - name: 🔍 Vérifier les fichiers
        run: |
          echo "Liste des fichiers dans le répertoire actuel :"
          ls -la
          echo "Vérification de l'existence du fichier .env :"
          if [ -f .env ]; then
            echo ".env a été créé avec succès."
          else
            echo "Erreur : le fichier .env n'existe pas."
            exit 1
          fi

      - name: 🚀 Transférer les fichiers au VPS
        uses: appleboy/scp-action@v0.1.9  # Utilisez une version plus récente
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: |
            docker-compose.yml
            .env
          target: /home/${{ secrets.VPS_USER }}/Boutique-NoName/
          port: 22
          timeout: 30s
          command_timeout: 10m
          use_insecure_cipher: false
          rm: false
          debug: true
          strip_components: 0
          overwrite: true

      - name: 🚀 Run Docker Compose on Production
        uses: appleboy/ssh-action@v0.1.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            mkdir -p /home/${{ secrets.VPS_USER }}/Boutique-NoName/
            cd /home/${{ secrets.VPS_USER }}/Boutique-NoName/
            docker-compose pull app
            docker-compose up -d --build
            docker system prune --all --volumes --force

      - name: 🧹 Cleanup .env
        run: rm .env
